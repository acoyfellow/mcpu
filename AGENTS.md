# Agent instructions

Keep mcpu tiny. Version stays `0.0.1`.

Describe what this commit does. Do not add README features that have no tool and no test.

Do not give the agent bash and mcpu on the same tree.

The draft is process memory. A grant is optional. No grant means the tool runs.

`GET /` is a reader. Writes go through tools.
